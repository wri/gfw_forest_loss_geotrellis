package org.globalforestwatch.layers

/*
import org.scalatest.FunSuite

class TreeCoverDensitySuits extends FunSuite {
  test("Threshold 0") {
    assert(TreeCover threshold(0) === 0)
    assert(threshold(9) === 0)
  }

  test("Threshold 10") {
    assert(threshold(10) === 10)
    assert(threshold(14) === 10)
  }

  test("Threshold 15") {
    assert(threshold(15) === 15)
    assert(threshold(19) === 15)
  }


  test("Threshold 20") {
    assert(threshold(20) === 20)
    assert(threshold(24) === 20)
  }

  test("Threshold 25") {
    assert(threshold(25) === 25)
    assert(threshold(29) === 25)
  }

  test("Threshold 30") {
    assert(threshold(30) === 30)
    assert(threshold(49) === 30)
  }

  test("Threshold 50") {
    assert(threshold(50) === 50)
    assert(threshold(74) === 50)
  }


  test("Threshold 75") {
    assert(threshold(75) === 75)
    assert(threshold(100) === 75)
  }
}
*/
